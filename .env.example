# ============================================================
#  UoK Student Performance Monitoring System — Environment Config
#  Copy this file to .env and fill in your actual values
# ============================================================

# Django
SECRET_KEY=your-very-secret-key-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Twilio SMS (get credentials from https://console.twilio.com)
TWILIO_ACCOUNT_SID=AC2a9610f7c017667a4efcf72864b5ce89
TWILIO_AUTH_TOKEN=6024014c4a83dc5a829818a2c3649cd0
TWILIO_PHONE_NUMBER=+17373162199

# Alert Thresholds (overrides settings.py defaults)
GRADE_ALERT_THRESHOLD=50
ATTENDANCE_ALERT_THRESHOLD=75

# Celery (using memory broker instead of Redis)
CELERY_BROKER_URL=memory://localhost/
CELERY_RESULT_BACKEND=django-db

# Email (optional — backup notification channel)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your@email.com
EMAIL_HOST_PASSWORD=your_email_password
DEFAULT_FROM_EMAIL=noreply@uok.ac.rw

# PostgreSQL (for production — SQLite is used by default)
# DB_NAME=student_monitor
# DB_USER=postgres
# DB_PASSWORD=your_db_password
# DB_HOST=localhost
# DB_PORT=5432